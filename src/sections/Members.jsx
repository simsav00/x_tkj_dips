import { Container } from "@src/components/common/Container"
import { SectionTitle } from "@src/components/common/SectionTitle"

export const Members = () => {

    return(
        <Container>
            <SectionTitle id="anggota">
                Anggota
            </SectionTitle>
        </Container>
    )
}